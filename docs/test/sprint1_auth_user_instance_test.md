# Sprint 1 功能测试文档 — 认证、用户、实例管理

> **测试版本：** SagittaDB v1.0-beta
> **对应计划：** Sprint 1 — 认证、用户管理、实例管理
> **测试环境：** http://localhost（前端）/ http://localhost:8000（后端）
> **前置条件：** Sprint 0 测试通过，系统已初始化（`POST /api/v1/system/init/`）
> **测试账号：** admin / Admin@2024!（默认密码会触发强制改密，建议首登改为 `Admin@2026A` 后继续后续用例）

---

## 测试范围

| 模块 | 覆盖内容 |
|---|---|
| 登录认证 | 账号密码登录、错误凭证拦截、Token 返回、默认/弱/过期密码强制改密 |
| JWT Token | access_token 有效性、refresh_token 刷新 |
| 双因素认证 | 2FA 开启流程、TOTP 验证 |
| 用户管理 | 用户 CRUD、权限授权、账号禁用 |
| 实例管理 | 实例 CRUD、测试连接、密码加密存储 |
| 资源组管理 | 资源组 CRUD、成员管理 |

---

## 一、登录认证测试

### TC-S1-001 默认密码首次登录强制改密

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-001 |
| **测试场景** | 使用默认管理员密码登录时必须先完成密码修改 |
| **测试步骤** | 1. 浏览器访问 http://localhost<br>2. 输入用户名 `admin`，密码 `Admin@2024!`<br>3. 点击登录<br>4. 页面进入强制改密表单，输入新密码 `Admin@2026A` 并确认<br>5. 使用 `admin / Admin@2026A` 重新登录 |
| **预期结果** | 第一次登录不签发正式 Token，并提示必须改密；改密成功后返回登录页，重新登录后跳转 Dashboard，顶部显示当前用户 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-001A 密码复杂度规则校验

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-001A |
| **测试场景** | 新密码必须满足系统统一密码安全规则 |
| **测试步骤** | 1. 进入强制改密或个人设置修改密码表单<br>2. 分别输入 `short1A!`、`password1!`、`Password!`、`Password1` 等不合规密码<br>3. 输入合规密码 `Admin@2026B` |
| **预期结果** | 不合规密码分别提示长度、数字、大写字母、小写字母或特殊字符缺失；合规密码可提交成功 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-002 错误密码拒绝登录

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-002 |
| **测试场景** | 错误密码不能登录 |
| **测试步骤** | 1. 输入用户名 `admin`，密码 `WrongPassword`<br>2. 点击登录 |
| **预期结果** | 显示错误提示"用户名或密码错误"，停留在登录页 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-003 不存在用户拒绝登录

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-003 |
| **测试场景** | 不存在的用户名无法登录 |
| **测试步骤** | 1. 输入用户名 `ghost_user`，任意密码<br>2. 点击登录 |
| **预期结果** | 返回 401，显示认证失败提示 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-004 Token 刷新

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-004 |
| **测试场景** | 使用 refresh_token 换取新的 access_token |
| **测试步骤** | 1. 使用已完成改密的账号 POST http://localhost:8000/api/v1/auth/login/ 获取 refresh_token<br>2. POST http://localhost:8000/api/v1/auth/token/refresh/ 传入 refresh_token |
| **预期结果** | 返回新的 access_token 和 refresh_token，HTTP 200 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-005 无效 Token 拒绝访问

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-005 |
| **测试场景** | 伪造或过期 Token 无法访问受保护接口 |
| **测试步骤** | 1. 携带 `Authorization: Bearer invalid.token.here` 访问 `GET /api/v1/auth/me/` |
| **预期结果** | 返回 HTTP 401 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-006 /me 接口返回当前用户信息

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-006 |
| **测试场景** | 登录后 /me 接口返回完整用户信息 |
| **测试步骤** | 1. 登录获取 access_token<br>2. GET http://localhost:8000/api/v1/auth/me/ 携带 Bearer Token |
| **预期结果** | 返回 username=admin、is_superuser=true、permissions 列表、tenant_id=1，并包含 `password_expiring_soon`、`days_until_password_expiry` 字段 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-007 登出接口

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-007 |
| **测试场景** | 登出后 Token 失效 |
| **测试步骤** | 1. 登录后点击右上角头像 → 退出登录<br>2. 尝试再次访问受保护页面 |
| **预期结果** | 成功退出，跳转至登录页，旧 Token 不可再用 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 二、双因素认证（2FA）测试

### TC-S1-008 2FA 开启流程

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-008 |
| **测试场景** | 用户开启 TOTP 双因素认证 |
| **测试步骤** | 1. 登录后进入"个人设置"→"安全设置"<br>2. 点击"开启双因素认证"<br>3. 使用 Google Authenticator 扫描二维码<br>4. 输入 App 生成的 6 位验证码确认激活 |
| **预期结果** | 显示绑定成功提示，下次登录需要输入 TOTP 验证码 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 三、用户管理测试

### TC-S1-009 创建新用户

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-009 |
| **测试场景** | 超管创建新用户并授权 |
| **测试步骤** | 1. 进入"系统管理"→"用户管理"<br>2. 点击"新建用户"<br>3. 填写用户名 `testuser`，密码 `Test@2024!`，邮箱 `test@example.com`<br>4. 勾选权限：`sql_submit`、`query_query`<br>5. 点击保存 |
| **预期结果** | 用户列表显示新用户 testuser，权限列显示已授权权限 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-010 新用户登录验证

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-010 |
| **测试场景** | 使用新建用户账号登录 |
| **测试步骤** | 1. 退出 admin 账号<br>2. 使用 testuser / Test@2024! 登录 |
| **预期结果** | 登录成功，进入 Dashboard，只显示 testuser 有权限的菜单项 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-011 禁用用户账号

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-011 |
| **测试场景** | 禁用用户后无法登录 |
| **测试步骤** | 1. admin 登录 → 用户管理 → 找到 testuser → 点击"禁用"<br>2. 尝试用 testuser 登录 |
| **预期结果** | 禁用成功，testuser 登录返回 401 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-012 修改用户密码

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-012 |
| **测试场景** | 个人设置中修改密码 |
| **测试步骤** | 1. 登录后进入"个人设置"→"修改密码"<br>2. 输入当前密码 `Admin@2026A`<br>3. 输入新密码 `Admin@2026B`，确认新密码<br>4. 提交 |
| **预期结果** | 提示修改成功，使用新密码可正常登录 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 四、实例管理测试

### TC-S1-013 创建 MySQL 实例

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-013 |
| **测试场景** | 创建 MySQL 数据库实例 |
| **测试步骤** | 1. 进入"实例管理"→"新建实例"<br>2. 填写：实例名 `test-mysql`，类型 master，数据库类型 mysql，主机 127.0.0.1，端口 3306，用户名 root，密码 root<br>3. 点击保存 |
| **预期结果** | 实例列表显示新实例，密码字段不明文显示 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-014 实例密码加密存储验证

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-014 |
| **测试场景** | 实例密码在数据库中以密文存储 |
| **测试步骤** | 1. 创建实例后<br>2. 执行 `docker compose exec postgres psql -U sagitta -d sagittadb -c "SELECT instance_name, password FROM sql_instance LIMIT 5;"` |
| **预期结果** | password 字段显示为 Fernet 加密密文（以 `gAAAAA` 开头），不是明文 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-015 实例列表查询

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-015 |
| **测试场景** | 实例列表分页展示 |
| **测试步骤** | 1. 进入"实例管理"页面<br>2. 查看列表是否显示已创建的实例<br>3. 测试搜索框，输入实例名关键字 |
| **预期结果** | 正确显示实例列表，搜索功能过滤正常 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-016 实例测试连接

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-016 |
| **测试场景** | 对已创建实例执行连接测试 |
| **测试步骤** | 1. 在实例列表中找到 test-mysql<br>2. 点击"测试连接"按钮 |
| **预期结果** | 连接成功显示数据库版本信息；若实例不可达，显示连接失败信息（非 500 错误） |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-017 删除实例

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-017 |
| **测试场景** | 删除（停用）实例 |
| **测试步骤** | 1. 实例列表中选择一个测试实例<br>2. 点击"删除"并确认 |
| **预期结果** | 实例从列表消失，或状态变为停用 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-S1-018 无 instance_manage 权限无法创建实例

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-018 |
| **测试场景** | 无权限用户无法创建实例 |
| **测试步骤** | 1. 使用 testuser（无 instance_manage 权限）登录<br>2. 尝试访问实例管理或创建实例 API |
| **预期结果** | 返回 403 禁止访问，或菜单不显示实例管理入口 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 五、资源组管理测试

### TC-S1-019 创建资源组

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-S1-019 |
| **测试场景** | 创建资源组并添加成员 |
| **测试步骤** | 1. 进入"系统管理"→"资源组"<br>2. 点击"新建"，填写资源组名称 `测试资源组`<br>3. 在成员管理穿梭框中将 testuser 加入右侧<br>4. 保存 |
| **预期结果** | 资源组创建成功，成员列表显示 testuser |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 测试汇总

| 总用例数 | 通过 | 失败 | 未测试 |
|---|---|---|---|
| 20 | 0 | 0 | 20 |

**测试人员：**
**测试日期：**
**备注：**
