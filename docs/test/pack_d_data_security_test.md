# Pack D 功能测试文档 — 数据脱敏、数据字典、工单模板、AI Text2SQL

> **测试版本：** SagittaDB v1.0-beta
> **对应计划：** Pack D — 数据脱敏、数据字典、SQL 工单模板、AI Text2SQL
> **测试环境：** http://localhost（前端）/ http://localhost:8000（后端）
> **前置条件：** Sprint 1~2、Pack A 测试通过，至少有一个可连接实例
> **测试账号：** admin / Admin@2024!（默认密码会触发强制改密）

---

## 测试范围

| 模块 | 覆盖内容 |
|---|---|
| 数据脱敏 | 规则创建（7 种类型）、查询结果脱敏、实时预览 |
| 数据字典 | 三级浏览、字段详情 |
| 工单模板 | 模板 CRUD、个人/全局、从模板创建、保存为模板、快速使用 |
| AI Text2SQL | 自然语言生成 SQL、方言适配 |

---

## 一、数据脱敏测试

### TC-D-001 创建邮箱脱敏规则

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-001 |
| **测试场景** | 创建一条邮箱字段脱敏规则 |
| **测试步骤** | 1. 进入"数据安全"→"数据脱敏"<br>2. 点击"新建规则"<br>3. 选择实例和数据库，表名 `users`，列名 `email`<br>4. 规则类型选"邮箱"<br>5. 保存 |
| **预期结果** | 脱敏规则创建成功，出现在规则列表 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-002 创建手机号脱敏规则

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-002 |
| **测试场景** | 创建手机号脱敏规则 |
| **测试步骤** | 1. 新建规则，选择实例，列名 `phone`<br>2. 规则类型选"手机号"<br>3. 保存 |
| **预期结果** | 规则创建成功 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-003 脱敏规则实时预览

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-003 |
| **测试场景** | 在规则配置页输入测试值，实时看到脱敏效果 |
| **测试步骤** | 1. 新建或编辑脱敏规则<br>2. 在"预览"输入框中输入 `test@example.com`<br>3. 观察脱敏后的值 |
| **预期结果** | 实时显示脱敏结果，如 `t***@example.com` |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-004 自定义正则脱敏规则

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-004 |
| **测试场景** | 创建自定义正则类型脱敏规则 |
| **测试步骤** | 1. 新建规则，类型选"自定义正则"<br>2. 填写正则 `\d{4}(?=\d{4})`，替换为 `****`<br>3. 预览输入 `6225880123456789`<br>4. 保存 |
| **预期结果** | 正则规则保存成功，预览显示脱敏效果 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-005 在线查询脱敏规则生效

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-005 |
| **测试场景** | 查询已配置脱敏规则的列时，结果自动脱敏 |
| **前置条件** | 已为 `users.email` 配置邮箱脱敏规则 |
| **测试步骤** | 1. 在线查询中执行 `SELECT id, email FROM users LIMIT 10;` |
| **预期结果** | email 列显示脱敏结果（如 `t***@e***.com`），id 列正常显示 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-006 脱敏规则精确匹配优先级

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-006 |
| **测试场景** | 表级规则优先于实例级规则 |
| **测试步骤** | 1. 同时配置实例级 email 规则（规则A）和表级 users.email 规则（规则B）<br>2. 查询 users 表 email 列 |
| **预期结果** | 应用表级规则B（更精确的规则优先） |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 二、数据字典测试

### TC-D-007 数据字典三级浏览

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-007 |
| **测试场景** | 数据字典左侧树形结构正常展开 |
| **测试步骤** | 1. 进入"运维中心"→"数据字典"<br>2. 左侧选择实例<br>3. 展开数据库节点<br>4. 点击一个表名 |
| **预期结果** | 右侧显示该表的字段详情（列名、数据类型、是否可空、默认值、注释），并可继续查看表约束与索引信息 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-008 数据字典实时从引擎查询

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-008 |
| **测试场景** | 数据字典实时查询，不使用缓存 |
| **测试步骤** | 1. 查看某表的字段信息<br>2. 在数据库中 ALTER TABLE 添加新列<br>3. 刷新数据字典 |
| **预期结果** | 刷新后立即显示新添加的列，不需要等待缓存过期 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-009 数据字典约束与索引展示

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-009 |
| **测试场景** | 数据字典展示主键、唯一键、外键及索引信息 |
| **测试步骤** | 1. 进入"运维中心"→"数据字典"<br>2. 选择一个带主键、唯一键、外键和联合索引的表<br>3. 查看右侧"表约束"与"索引信息"区域 |
| **预期结果** | 表约束区域正确显示主键/唯一键/外键及引用关系；索引区域正确显示索引类型、是否联合索引和索引列 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-010 数据字典长文本布局

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-010 |
| **测试场景** | 长注释或长默认值不会导致列名等字段被挤压折行 |
| **测试步骤** | 1. 选择一个注释较长的表字段<br>2. 观察字段详情表格布局<br>3. 横向滚动并悬浮查看长文本 |
| **预期结果** | 字段详情表格支持横向滚动；列名、默认值、注释保持单行省略显示；悬浮可查看完整内容 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 三、工单模板测试

### TC-D-011 创建全局工单模板

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-011 |
| **测试场景** | DBA/管理员创建一个全局 SQL 工单模板 |
| **测试步骤** | 1. 进入"SQL 工单"→"工单模板"<br>2. 点击"新建模板"<br>3. 填写模板名称 `常用查询模板`，SQL 内容为多行 SQL 示例<br>4. 范围选"全局模板"<br>5. 保存 |
| **预期结果** | 模板创建成功，所有有权限用户可见 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-012 创建个人工单模板

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-012 |
| **测试场景** | 个人模板只有创建者可见 |
| **测试步骤** | 1. 创建模板，范围选"我的模板"<br>2. 用另一个账号登录查看模板列表 |
| **预期结果** | 个人模板不出现在其他用户的模板列表中 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-013 从模板快速创建工单

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-013 |
| **测试场景** | 点击模板"使用"按钮跳转到提交页并填充模板内容 |
| **测试步骤** | 1. 进入工单模板列表<br>2. 找到 `常用查询模板`，点击"使用"按钮 |
| **预期结果** | 跳转至工单提交页，工单标题、审批流、SQL 编辑器等字段已按模板填充 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-014 模板使用次数统计

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-014 |
| **测试场景** | 每次点击"使用"后使用次数递增 |
| **测试步骤** | 1. 记录模板当前使用次数<br>2. 点击"使用"两次<br>3. 刷新模板列表 |
| **预期结果** | 使用次数增加 2 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-015 保存当前工单为模板

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-015 |
| **测试场景** | 在提交工单页将当前填写内容保存为模板 |
| **测试步骤** | 1. 进入"SQL 工单"→"提交工单"<br>2. 填写工单名称、SQL 内容、实例、数据库、审批流<br>3. 点击"保存为模板"<br>4. 补充模板名称、分类、范围并保存 |
| **预期结果** | 模板保存成功，并出现在工单模板列表中 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 四、AI Text2SQL 测试

### TC-D-016 AI 生成 SQL（需配置 API Key）

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-016 |
| **测试场景** | 输入自然语言描述，AI 生成对应方言 SQL |
| **前置条件** | 系统配置中已填写有效的 Anthropic API Key |
| **测试步骤** | 1. 进入工单提交页或在线查询页<br>2. 点击"AI 生成 SQL"按钮<br>3. 输入描述：`查询最近7天创建的用户，按创建时间倒序，显示id、用户名、邮箱`<br>4. 点击生成 |
| **预期结果** | 生成 SQL 插入到编辑器，语法符合目标实例的数据库方言 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-D-017 未配置 API Key 时给出友好提示

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-D-017 |
| **测试场景** | 未配置 AI API Key 时点击 AI 生成按钮 |
| **测试步骤** | 1. 确保系统配置中 AI API Key 为空<br>2. 点击"AI 生成 SQL"按钮 |
| **预期结果** | 给出提示"请先在系统配置中配置 AI API Key"，不报 500 错误 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 测试汇总

| 总用例数 | 通过 | 失败 | 未测试 |
|---|---|---|---|
| 17 | 0 | 0 | 17 |

**测试人员：**
**测试日期：**
**备注：**
