# Pack F 功能测试文档 — 第三方登录（LDAP/钉钉/飞书/企微/OIDC）

> **测试版本：** SagittaDB v1.0-beta
> **对应计划：** Pack F — 第三方登录集成（LDAP 企业目录 + 4 种 OAuth2/OIDC）
> **测试环境：** http://localhost（前端）/ http://localhost:8000（后端）
> **前置条件：** Sprint 1 测试通过，各平台应用已创建并获得相应 AppKey/Secret
> **测试账号：** admin / Admin@2024!

---

## 测试范围

| 模块 | 覆盖内容 |
|---|---|
| LDAP 登录 | 三步验证、用户自动创建、配置管理 |
| 钉钉扫码登录 | OAuth2 授权流程、用户 provision |
| 飞书扫码登录 | OIDC 流程、token 交换 |
| 企业微信扫码登录 | qrConnect 流程 |
| OIDC 通用 SSO | Keycloak/Okta 等标准 OIDC |
| 安全机制 | state 防 CSRF、回调 URL 验证 |

---

## 一、LDAP 登录测试

### TC-F-001 LDAP 配置保存

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-001 |
| **测试场景** | 系统配置中保存 LDAP 配置 |
| **前置条件** | 有可连接的 LDAP 服务器 |
| **测试步骤** | 1. 进入"系统配置"→"LDAP 认证"Tab<br>2. 填写服务器地址、Bind DN、密码、搜索 Base DN、用户过滤器<br>3. 配置属性映射（uid→username，mail→email，cn→display_name）<br>4. 保存 |
| **预期结果** | 配置保存成功，密码字段显示为 `******` |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-002 LDAP 用户登录

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-002 |
| **测试场景** | LDAP 用户使用域账号登录 |
| **前置条件** | LDAP 配置已保存，LDAP 服务器可连接 |
| **测试步骤** | 1. 登录页切换至"LDAP 登录"方式<br>2. 输入 LDAP 用户名和密码<br>3. 点击登录 |
| **预期结果** | 登录成功，用户自动创建（auth_type=ldap），跳转 Dashboard |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-003 LDAP 用户自动 provision

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-003 |
| **测试场景** | LDAP 首次登录自动在本地创建用户记录 |
| **测试步骤** | 1. 使用 LDAP 中存在但本地不存在的账号登录<br>2. 登录成功后，检查用户管理列表 |
| **预期结果** | 用户管理中出现新用户，auth_type 显示为 ldap |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-004 LDAP 密码错误被拒绝

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-004 |
| **测试场景** | LDAP 密码错误时无法登录 |
| **测试步骤** | 1. 使用正确 LDAP 用户名，错误密码登录 |
| **预期结果** | 返回 401，提示认证失败 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-005 LDAP 未启用时按钮不显示

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-005 |
| **测试场景** | LDAP 未启用时，登录页不显示 LDAP 入口 |
| **测试步骤** | 1. 系统配置中关闭 LDAP 启用开关<br>2. 访问登录页 |
| **预期结果** | 登录页不显示 LDAP 登录选项 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 二、钉钉扫码登录测试

### TC-F-006 钉钉登录配置保存

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-006 |
| **测试场景** | 保存钉钉登录应用配置 |
| **前置条件** | 已在钉钉开放平台创建扫码登录应用 |
| **测试步骤** | 1. 系统配置 → 钉钉 Tab<br>2. 填写登录 AppKey、登录 AppSecret<br>3. 开启"钉钉登录"开关<br>4. 保存 |
| **预期结果** | 配置保存成功，登录页出现钉钉扫码入口 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-007 钉钉授权 URL 生成

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-007 |
| **测试场景** | 点击钉钉登录按钮生成正确的授权 URL |
| **测试步骤** | 1. 登录页点击"钉钉扫码登录"<br>2. 观察跳转 URL |
| **预期结果** | 跳转到 `login.dingtalk.com/oauth2/auth`，URL 中包含 client_id、redirect_uri、state 参数 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-008 钉钉完整登录流程

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-008 |
| **测试场景** | 完整的钉钉扫码登录流程 |
| **前置条件** | 钉钉应用配置正确，回调 URL 已在钉钉后台注册 |
| **测试步骤** | 1. 点击"钉钉扫码登录"<br>2. 扫描二维码授权<br>3. 观察回调处理 |
| **预期结果** | 登录成功，跳转至 Dashboard，用户自动创建（auth_type=dingtalk） |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 三、飞书扫码登录测试

### TC-F-009 飞书登录配置保存

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-009 |
| **测试场景** | 保存飞书登录应用配置并启用 |
| **测试步骤** | 1. 系统配置 → 飞书 Tab<br>2. 填写 App ID、App Secret<br>3. 开启"飞书登录"开关<br>4. 保存 |
| **预期结果** | 登录页出现飞书扫码入口 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-010 飞书完整登录流程

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-010 |
| **测试场景** | 飞书扫码完整授权流程 |
| **前置条件** | 飞书应用配置正确 |
| **测试步骤** | 1. 点击"飞书扫码登录"<br>2. 扫码授权<br>3. 等待回调 |
| **预期结果** | 登录成功，用户自动 provision（auth_type=feishu） |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 四、企业微信扫码登录测试

### TC-F-011 企微登录配置保存

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-011 |
| **测试场景** | 保存企业微信登录配置 |
| **测试步骤** | 1. 系统配置 → 企业微信 Tab<br>2. 填写 CorpID、AgentId、AppSecret<br>3. 开启"企微登录"开关<br>4. 保存 |
| **预期结果** | 配置保存成功，登录页出现企微入口 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-012 企微完整登录流程

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-012 |
| **测试场景** | 企业微信扫码完整登录流程 |
| **前置条件** | 企微应用配置正确，已在企微后台配置可信域名 |
| **测试步骤** | 1. 点击"企微扫码登录"<br>2. 企微扫码授权 |
| **预期结果** | 登录成功（auth_type=wecom） |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 五、OIDC 通用 SSO 测试

### TC-F-013 OIDC 配置保存

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-013 |
| **测试场景** | 配置 OIDC Provider 信息 |
| **前置条件** | 有可用的 OIDC Provider（如 Keycloak） |
| **测试步骤** | 1. 系统配置 → OIDC 登录 Tab<br>2. 填写 Client ID、Client Secret、授权端点、Token 端点、UserInfo 端点<br>3. 开启 OIDC 登录<br>4. 保存 |
| **预期结果** | 配置保存成功，登录页出现 OIDC 登录入口 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-014 OIDC 完整登录流程

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-014 |
| **测试场景** | OIDC authorization_code 流程完整验证 |
| **前置条件** | OIDC Provider 配置正确 |
| **测试步骤** | 1. 点击 OIDC 登录入口<br>2. 在 Provider 页面完成认证<br>3. 回调至 `/oauth/callback` |
| **预期结果** | 登录成功，前端读取 token 并跳转 Dashboard |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 六、安全机制测试

### TC-F-015 OAuth state 防 CSRF 验证

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-015 |
| **测试场景** | 伪造或重放 state 参数被拒绝 |
| **测试步骤** | 1. 直接访问回调 URL，携带伪造的 state 参数：`GET /api/v1/auth/dingtalk/callback/?code=xxx&state=fake_state` |
| **预期结果** | 返回 400 或 401，提示 state 无效 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-016 OAuth 回调页面正常处理 token

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-016 |
| **测试场景** | 前端 /oauth/callback 页面正确读取 token 并跳转 |
| **测试步骤** | 1. 模拟后端回调：访问 `http://localhost/oauth/callback?access_token=xxx&refresh_token=yyy`<br>2. 观察页面行为 |
| **预期结果** | 页面自动读取 token，调用 /auth/me/ 验证，跳转 Dashboard；若 token 无效则跳回登录页 |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

### TC-F-017 未启用的 Provider 返回明确错误

| 项目 | 内容 |
|---|---|
| **用例编号** | TC-F-017 |
| **测试场景** | 未启用的 OAuth provider 授权接口返回正确错误 |
| **测试步骤** | 1. 确保飞书登录开关已关闭<br>2. 访问 `GET /api/v1/auth/feishu/authorize/` |
| **预期结果** | 返回 400，提示"飞书登录未启用" |
| **实际结果** | |
| **状态** | ⬜ 未测试 |

---

## 测试汇总

| 总用例数 | 通过 | 失败 | 未测试 |
|---|---|---|---|
| 17 | 0 | 0 | 17 |

**测试人员：**
**测试日期：**
**备注：** 第三方登录测试需要在对应平台创建应用并配置回调 URL 为 `http://localhost:8000/api/v1/auth/{provider}/callback/`
